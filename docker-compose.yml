services:
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: rico-backend
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - "8000:8080"
