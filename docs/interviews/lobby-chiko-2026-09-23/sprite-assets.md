# 스프라이트 에셋

> Source: Deep Interview, 2026-09-23

## Key Points
- 원본: 단일 시트 PNG (1000x1950, RGBA), 5방향 (모두 오른쪽 기준)
  - 윗줄: 정면 / 옆면(우) / 뒷면
  - 아랫줄: 대각선 앞(우) / 대각선 뒤(우)
- 원본 경로(로컬): `~/Downloads/IMG_0432.PNG`
- 리포지토리에 **방향별로 분리된 개별 파일**로 저장
- 왼쪽 방향 = 오른쪽 이미지 **좌우 반전(mirror)**, 반전본도 **별도 파일로 저장**
  - 결과: 8방향 파일 (정면, 뒷면, 우, 좌, 우앞, 좌앞, 우뒤, 좌뒤)
- 우선 4개 미니게임 모두 **동일한 치코 리소스** 사용

## 저장 규칙 (확정)
- 경로: `frontend/public/pages/lobby/chiko/`
- 포맷: **투명 WebP**
- 처리: 시트에서 각 포즈 크롭 → 여백 트림 → **동일 캔버스 크기**로 패딩, **발 끝(bottom-center) 정렬**
- 파일명(제안):
  - `front.webp`, `back.webp`
  - `side-right.webp`, `side-left.webp`
  - `diag-front-right.webp`, `diag-front-left.webp`
  - `diag-back-right.webp`, `diag-back-left.webp`
- 4개 게임 모두 이 한 세트를 참조. 코드에서는 게임 ID → 스프라이트 세트 매핑 테이블을 두어, 추후 코스튬 교체 시 매핑만 바꾸면 되게 함

## 확인된 사실
- 원본 시트 배경은 이미 **투명** (alpha=0) → 배경 제거 불필요
- 시트에 작은 분홍 점 잡티 2개 존재 (좌상단 ~(5,270), 중앙 ~(583,415)) → 전체 bbox 트림 대신 **포즈별 영역 크롭** 후 트림해야 함

## Open Questions
- 출력 해상도 (표시 크기의 2x 기준, 예: 높이 256~320px)

## 분리 결과 (2026-09-23)
- 8장 저장 완료: `frontend/public/pages/lobby/chiko/*.webp` (무손실 WebP, 각 38~55KB)
- 공통 캔버스 349x320, 전 포즈 동일 배율, 발 끝 하단 정렬(여백 4px 기준) + 가로 중앙 정렬
- 원본 포즈 최대 높이 ~261px → 약 1.19배 업스케일. 더 선명한 결과가 필요하면 고해상도 원본 필요
